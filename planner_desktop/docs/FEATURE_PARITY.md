# Feature parity: старое Flet-приложение → новый PySide6/QML десктоп

Инвентаризация по файлам старого UI (`ui/pages/today.py`,
`ui/pages/calendar.py`, `ui/dialogs.py`, `ui/daily_tasks.py`). Старые файлы
использованы **только как справка по поведению** — они не изменялись.

Статусы: `implemented` — есть в новом приложении; `partial` — есть в
урезанном виде; `missing` — нет, планируется; `deferred` — сознательно
отложено (с причиной).

Колонка «Фаза» указывает фазу продуктового роадмапа
([`PRODUCT_ROADMAP.md`](PRODUCT_ROADMAP.md)), в которой фича реализована
или запланирована: **Фаза 1** — UX ежедневного использования;
**Фаза 2** — полноценный календарный UI; **Фаза 3** — паритет функций
задач; **Фаза 4** — продуктизация синхронизации; **Фаза 5** — миграция
и релиз. Для уже реализованного в ранних итерациях указана Фаза 1
(историческая маркировка MVP/Phase 2/Phase 3 старых итераций заменена
на фазы роадмапа).

> Визуальный апгрейд MVP-интерфейса (дизайн-система, тени, иконки, правая
> сводка «Сегодня», компактный Quick Add) описан в
> [`UI_POLISH.md`](UI_POLISH.md). Бизнес-логика и контракты не менялись.
>
> Юзабилити-итерация фазы 2 (инспектор задачи, умный Quick Add с
> приоритетом, реальные ежедневные задачи, агенда календаря, клавиатурные
> сокращения, живые пустые состояния) описана в
> [`PHASE2_USABILITY.md`](PHASE2_USABILITY.md).
>
> Итерация «История + фильтры календаря + диагностика» добавила: реальную
> страницу «История» (журнал выполненного из разовых задач по
> `Task.completed_at` и отметок ежедневных, фильтр 7/30/всё, «вернуть
> в работу»), фильтры агенды календаря (все / активные / выполненные /
> ежедневные) с чек-листом ежедневных на выбранный день и инспектором,
> разбивку очереди и панель «Диагностика» в настройках. Схема БД v3 → v4
> (аддитивная колонка `tasks.completed_at`, backfill из `updated_at`).

## Страница «Сегодня»

| Старая фича (ui/pages/today.py) | Статус в новом | Фаза |
|---|---|---|
| Быстрый ввод: название | implemented | MVP |
| Быстрый ввод: дата/время/длительность | implemented (текстовые поля + кнопки «Сегодня»/«Завтра») | MVP |
| Быстрый ввод: чекбокс «в календарь» | implemented (в старом дата сама делала задачу календарной) | MVP |
| Быстрый ввод: all-day задача | implemented (в старом app all-day создавался неявно «дата без времени») | MVP |
| Быстрый ввод: приоритет | implemented — компактный чип приоритета в строке Quick Add (меню 0–3), `addQuick`/`addTaskDetailed` | Phase 2 |
| Быстрый ввод: разбор текста | implemented — лёгкий парсер «сегодня/завтра/послезавтра» + ЧЧ:ММ (quick_parse), консервативный | Phase 2 |
| DatePicker / TimePicker (нативные пикеры) | missing — план фазы 1: DatePickerField (месячная сетка, русская локаль), TimePickerField (клавиатура + список), DurationPicker (пресеты 15–120 мин + своя) в редакторе и Quick Add | Фаза 1 |
| Список «Сегодня» | implemented | MVP |
| Список «Без даты» | implemented | MVP |
| Галочка выполнено/не выполнено | implemented (локально, в календарь не уходит — решение фазы 1) | MVP |
| Редактирование задачи (диалог) | implemented (TaskEditorDialog: название, заметки, приоритет, дата, время, длительность, весь день, выполнено) | MVP |
| Удаление с подтверждением | implemented (ConfirmDialog, тумбстоун) | MVP |
| Маркер приоритета + пилюля | implemented (PriorityPill, цвета как в core/priorities.py) | MVP |
| Значок «Google» у связанной задачи | implemented (бейдж «Синк…» у задач с pending-операцией; бейдж «Google» у привязанных) | MVP |
| Заметки задачи (превью в карточке) | implemented | MVP |
| Тосты об успехе/ошибке | implemented (toastMessage у ViewModel + всплывашка в QML) | MVP |
| Авто-push в Google после правки | deferred — автосинка в новом приложении нет вообще (жёсткое требование); push копится в локальной очереди, отправка — вручную; opt-in фоновый синк | Фаза 4 |

## Ежедневные задачи (ui/daily_tasks.py)

| Старая фича | Статус в новом | Фаза |
|---|---|---|
| Чек-лист на сегодня (отметить/снять) | implemented — реальные ежедневные из SQLite, отметки по датам (desktop_daily_completions) | Phase 2 |
| CRUD ежедневных задач (создать/редактировать/удалить) | implemented — DailyTasksDialog + DailyTasksViewModel + DailyTaskService (тумбстоун при удалении) | Phase 2 |
| Маска дней недели (Пн…Вс) | implemented — 7-битная маска, чипы дней + пресеты «Будни/Выходные/Каждый день» | Phase 2 |
| Rollover в полночь + статус inactive | implemented иначе — отметка хранится по дате, «rollover» не нужен (каждый день чистый); enabled/disabled есть | Phase 2 |

## Страница «Календарь» (ui/pages/calendar.py)

| Старая фича | Статус в новом | Фаза |
|---|---|---|
| Недельная навигация (◀ / Сегодня / ▶) | implemented | MVP |
| Заголовок «Неделя dd.mm — dd.mm.yyyy» | implemented | MVP |
| Полоса дней недели с количеством задач | implemented (выбор дня кликом) | MVP |
| Список задач выбранного дня | implemented (агенда: временная колонка + линия-таймлайн + карточки TaskCard, сортировка «весь день» → по времени) | Фаза 1 |
| Сводка выбранного дня | implemented (бейдж «N из M готово», кнопка «Задача») | Phase 2 |
| Создание задачи на выбранный день | implemented (кнопка «+ Задача» открывает редактор с подставленной датой) | MVP |
| Почасовая сетка недели (слоты 00–23) | deferred — есть агенда со временем; полноценная сетка со слотами — отдельная фаза | Фаза 2 |
| Drag-and-drop «Без даты» → слот | deferred — требует почасовой сетки | Фаза 2 |
| Контекстное меню чипа (Snooze +30 / вечером / завтра) | missing — план фазы 1: меню снуза на карточке и в инспекторе: «Позже сегодня» / «Завтра» / «Следующая неделя» / «Выбрать дату и время» / «Без даты» (правила — domain/scheduling.py) | Фаза 1 |
| Панель «Без даты» рядом с сеткой | partial — задачи без даты видны на «Сегодня»; в календаре их пока нет | Фаза 2 |
| Автопрокрутка к текущему часу | deferred — нет почасовой сетки | Фаза 2 |
| Pull из Google при открытии страницы | deferred — автосинка нет сознательно; opt-in фоновый синк — фаза продуктизации синка | Фаза 4 |
| Фильтр задач дня (все / активные / выполненные) | implemented — сегментированный фильтр над агендой + счётчики; сводка дня от фильтра не зависит | Phase 2 |
| Ежедневные задачи в календаре | implemented — режим «Ежедневные»: чек-лист на выбранный день (по маске дней недели), отметка ставится на выбранную дату, не только на сегодня | Phase 2 |
| Инспектор выбранной задачи | implemented — тот же TaskInspector, что на «Сегодня», в правой колонке (широкий экран); при пустом выборе — сводка дня | Phase 2 |
| Навигация по дням стрелками ← / → | implemented — работает вне текстовых полей, переваливает через границы недели | Phase 2 |

## Диалоги (ui/dialogs.py)

| Старая фича | Статус в новом | Фаза |
|---|---|---|
| AlertDialog редактирования | implemented (TaskEditorDialog.qml, единый для создания и правки) | MVP |
| Подтверждение удаления | implemented (ConfirmDialog.qml) | MVP |
| Esc / клик мимо закрывают диалог | implemented (стандартное поведение Dialog в Qt Quick) | MVP |
| Overlay-слои planner_layer/backdrop | не нужно — у Qt Quick собственный Overlay | — |

## Переходы состояния задачи

| Переход | Статус | Примечание |
|---|---|---|
| Создать без даты | implemented | локальная задача, операций в очереди нет |
| Создать с датой и временем | implemented | ставит `create` в локальную Calendar-очередь |
| Создать all-day | implemented | ставит `create` (форма date/date) |
| Правка текста/приоритета | implemented | у синхронизированной задачи ставит `update` |
| Перенос даты/времени | implemented | через редактор |
| Без даты → запланирована | implemented | ставит `create` |
| Запланирована → без даты (unschedule) | partial | поддержано для задач, чьё событие ещё не допушено (снимается pending `create`), и для привязанных одиночных событий (ставится `delete` события, задача отвязывается и остаётся локальной). Для **экземпляра повторяющегося события** — сознательно запрещено с сообщением «пока не поддерживается» (уроки dead-letter старого приложения). |
| Выполнено/не выполнено | implemented | локально; событие в календаре не трогается (фаза 1) |
| Удаление | implemented | тумбстоун + `delete` в очередь, если событие существовало |

## Прочее

| Старая фича | Статус | Фаза |
|---|---|---|
| История выполненных задач | implemented — журнал по датам («Сегодня»/«Вчера»/полная дата) из двух локальных источников: разовые задачи (`Task.completed_at`, для выполненных до миграции — `updated_at`) и отметки ежедневных; фильтр 7 дней / 30 дней / всё; «вернуть в работу» для разовых (ежедневные отметки — только просмотр); «Подробнее» открывает общий редактор | Phase 2 |
| Настройки: статус синка | implemented (путь БД, pending/dead-letter счётчики, курсор, разбивка очереди create/update/delete, время последнего локального изменения) | MVP |
| Настройки: диагностика | implemented — версия схемы, счётчики задач/ежедневных/очереди, кнопка «Копировать» (без токенов и личных данных) | Phase 2 |
| Настройки: кнопка «Синхронизировать сейчас» | implemented — активна, когда есть изолированный токен и синк не выполняется; работа в фоновом потоке, прогресс/итог/ошибка и время последнего успешного синка на странице | Фаза 1 |
| Настройки: «Подключить Google Calendar» | implemented — явный браузерный OAuth (тестовый аккаунт), токен сохраняется ТОЛЬКО в `<PlannerDesktop>/token.json`; без client_secret.json кнопка выключена с подсказкой пути | Фаза 1 |
| Реальный Google-синк (OAuth, сеть) | implemented — `GoogleCalendarGateway` по контракту CalendarGateway + `ManualSyncService` (общий для кнопки и CLI `python -m scripts.desktop_calendar_sync_once --real-google`); только ручной запуск, см. GOOGLE_SYNC_SETUP.md | Фаза 1 |
| Автоматический синк при старте/по таймеру | deferred — сейчас никакого автосинка; станет opt-in переключателем (выключен по умолчанию) в фазе продуктизации синка | Фаза 4 |
