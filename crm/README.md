# CRM scripts

This folder contains CRM-specific helpers and sync utilities that are not present elsewhere in the project.
An audit for overlapping functionality with existing scripts in the repository showed no prior implementations
of the email normalization, organization name canonicalization, registry sync, contract migration, or duplicate
merge logic introduced here.

